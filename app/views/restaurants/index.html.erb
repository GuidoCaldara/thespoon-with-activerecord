<% @restaurants.each do |restaurant| %>
  <h1><%= restaurant.name %></h1>
  <%= link_to "See the Restaurant Page", restaurant_path(restaurant), method: :get %>
  <%= link_to "Destroy Restaurant", restaurant_path(restaurant), method: :delete %>
<% end %>
